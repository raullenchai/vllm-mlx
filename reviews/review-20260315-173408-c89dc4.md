OpenAI Codex v0.107.0 (research preview)
--------
workdir: /Users/raullenstudio/work/vllm-mlx
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: none
reasoning summaries: none
session id: 019cf411-b629-7473-b03b-d371fbca188b
--------
user
changes against 'main'
mcp startup: no servers
mcp startup: no servers
exec
/bin/zsh -lc 'git diff 6a8ecec533dd97b511d780cebe81689773f2ce00' in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:
diff --git a/tests/test_model_auto_config.py b/tests/test_model_auto_config.py
new file mode 100644
index 0000000..b44f21f
--- /dev/null
+++ b/tests/test_model_auto_config.py
@@ -0,0 +1,95 @@
+"""Tests for model auto-config detection."""
+
+import pytest
+
+from vllm_mlx.model_auto_config import ModelConfig, detect_model_config
+
+
+class TestDetectModelConfig:
+    """Test detect_model_config with various model paths."""
+
+    # Qwen family
+    @pytest.mark.parametrize(
+        "model_path",
+        [
exec
/bin/zsh -lc "sed -n '1,220p' vllm_mlx/model_auto_config.py" in /Users/raullenstudio/work/vllm-mlxexec
/bin/zsh -lc 'rg -n "tool_call_parser|reasoning_parser" vllm_mlx/server.py' in /Users/raullenstudio/work/vllm-mlx succeeded in 52ms:
"""Auto-detect optimal parser configuration from model name/path.

When users don't specify --tool-call-parser or --reasoning-parser,
this module infers the best configuration from the model name pattern.
"""

import logging
import re
from dataclasses import dataclass

logger = logging.getLogger(__name__)


@dataclass
class ModelConfig:
    """Auto-detected parser configuration for a model family."""

    tool_call_parser: str | None = None
    reasoning_parser: str | None = None

 succeeded in 51ms:
168:_reasoning_parser = None  # ReasoningParser instance when enabled
169:_reasoning_parser_name: str | None = None  # Parser name (e.g., "minimax")
173:_tool_call_parser: str | None = None  # Parser name: auto, mistral, qwen, llama, hermes
483:    if not _enable_auto_tool_choice or not _tool_call_parser:
484:        if _reasoning_parser_name and request and request.tools:
486:            inferred = _PARSER_MAP.get(_reasoning_parser_name)
514:            parser_cls = ToolParserManager.get_tool_parser(_tool_call_parser)
520:            logger.info(f"Initialized tool call parser: {_tool_call_parser}")
523:                f"Failed to initialize tool parser '{_tool_call_parser}': {e}"
608:    if not _enable_auto_tool_choice or not _tool_call_parser:
612:        parser_cls = ToolParserManager.get_tool_parser(_tool_call_parser)
617:            f"Tool parser '{_tool_call_parser}' not found. "
777:        logger.info(f"Native tool format enabled for parser: {_tool_call_parser}")
780:    if _enable_tool_logits_bias and _enable_auto_tool_choice and _tool_call_parser:
800:                factory = _make_factory(_tool_call_parser, tokenizer)
804:                logger.info(f"Tool logits bias enabled for parser: {_tool_call_parser}")
1757:    if request.tools and _tool_call_parser:
1759:    elif _reasoning_parser_name == "minimax":
2003:    if _reasoning_parser and not tool_calls:
2005:        reasoning_text, cleaned_text = _reasoning_parser.extract_reasoning(
exec
/bin/zsh -lc "git grep -n \"deepseek_r1\\|qwen3\\|glm47\\|harmony\\|minimax\\|hermes\"" in /Users/raullenstudio/work/vllm-mlx succeeded in 50ms:
.github/workflows/ci.yml:85:            tests/test_harmony_parsers.py \
README.md:37:rapid-mlx serve mlx-community/Qwen3.5-9B-4bit --tool-call-parser hermes --port 8000
README.md:171:rapid-mlx serve mlx-community/Qwen3.5-4B-MLX-4bit --tool-call-parser hermes --port 8000
README.md:174:rapid-mlx serve mlx-community/Qwen3.5-9B-4bit --tool-call-parser hermes --port 8000
README.md:177:rapid-mlx serve mlx-community/Qwen3.5-35B-A3B-8bit --tool-call-parser hermes --prefill-step-size 8192 --port 8000
README.md:180:rapid-mlx serve nightmedia/Qwen3.5-122B-A10B-Text-mxfp4-mlx --tool-call-parser hermes --kv-bits 8 --prefill-step-size 8192 --port 8000
README.md:183:rapid-mlx serve lmstudio-community/Qwen3-Coder-Next-MLX-4bit --tool-call-parser hermes --prefill-step-size 8192 --port 8000
README.md:194:| Qwen3.5 (all sizes) | `hermes` | `qwen3` | **Recommended** — 100% tool calling |
README.md:195:| Qwen3-Coder-Next | `hermes` | *(none)* | Fast coding, non-thinking mode |
README.md:196:| GLM-4.7 | `glm47` | *(none)* | 100% tool calling |
README.md:197:| MiniMax-M2.5 | `minimax` | `minimax` | XML tool format |
README.md:199:| DeepSeek-R1 | `deepseek` | `deepseek_r1` | With reasoning |
README.md:201:| Mistral | `hermes` | *(none)* | Hermes-compatible |
README.md:548:| `--tool-call-parser` | Parser: `hermes`, `minimax`, `qwen`, `llama`, `deepseek`, etc. | *(none)* |
README.md:549:| `--reasoning-parser` | Parser: `qwen3`, `deepseek_r1`, `minimax`, `gpt_oss` | *(none)* |
ROADMAP.md:59:| 10 | GLM-4.7-Flash 9B | 9B | 30.1 GB (8bit) | Prompt cache + KV quant + glm47 parser | TBD | **Benchmarked** ✅ 60 tok/s, 100% tool, 0% leak |
ROADMAP.md:69:| 20 | GLM-4.5-Air | MoE? | 56.4 GB | Prompt cache + KV quant + glm47 parser | TBD | **Benchmarked** ⚠️ ~46 tok/s, 100% tool, long gen broken |
ROADMAP.md:83:2. Apply best tool parser → `hermes`
ROADMAP.md:84:3. Apply best reasoning parser → `qwen3`
docs/development/architecture.md:139:├── reasoning_parsers/    # Reasoning parsers (qwen3, deepseek_r1)
exec
/bin/zsh -lc "sed -n '70,100p' vllm_mlx/reasoning/__init__.py" in /Users/raullenstudio/work/vllm-mlxexec
/bin/zsh -lc "sed -n '188,205p' README.md" in /Users/raullenstudio/work/vllm-mlxexec
/bin/zsh -lc "sed -n '814,860p' vllm_mlx/cli.py" in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:

<details>
<summary><strong>Other model families (DeepSeek, Mistral, GLM, Llama)</strong></summary>

| Model Family | `--tool-call-parser` | `--reasoning-parser` | Notes |
|-------------|---------------------|---------------------|-------|
| Qwen3.5 (all sizes) | `hermes` | `qwen3` | **Recommended** — 100% tool calling |
| Qwen3-Coder-Next | `hermes` | *(none)* | Fast coding, non-thinking mode |
| GLM-4.7 | `glm47` | *(none)* | 100% tool calling |
| MiniMax-M2.5 | `minimax` | `minimax` | XML tool format |
| GPT-OSS | `seed_oss` | `gpt_oss` | Native format |
| DeepSeek-R1 | `deepseek` | `deepseek_r1` | With reasoning |
| Llama 3.x | `llama` | *(none)* | JSON tool format |
| Mistral | `hermes` | *(none)* | Hermes-compatible |

All 17 parsers include automatic recovery — if a quantized model outputs broken tool calls as text, they're auto-converted back to structured format.

</details>
 succeeded in 53ms:
    )
    serve_parser.add_argument(
        "--tool-call-parser",
        type=str,
        default=None,
        choices=[
            "auto",
            "mistral",
            "qwen",
            "qwen3_coder",
            "llama",
            "hermes",
            "deepseek",
            "kimi",
            "granite",
            "nemotron",
            "xlam",
            "functionary",
            "glm47",
            "minimax",
 succeeded in 53ms:
    Returns:
        List of registered parser names.
    """
    return list(_REASONING_PARSERS.keys())


def _register_builtin_parsers():
    """Register built-in parsers."""
    from .deepseek_r1_parser import DeepSeekR1ReasoningParser
    from .gpt_oss_parser import GptOssReasoningParser
    from .harmony_parser import HarmonyReasoningParser
    from .minimax_parser import MiniMaxReasoningParser
    from .qwen3_parser import Qwen3ReasoningParser

    register_parser("qwen3", Qwen3ReasoningParser)
    register_parser("deepseek_r1", DeepSeekR1ReasoningParser)
    register_parser("gpt_oss", GptOssReasoningParser)
    register_parser("harmony", HarmonyReasoningParser)
    register_parser("minimax", MiniMaxReasoningParser)

exec
/bin/zsh -lc "sed -n '3178,3192p' vllm_mlx/server.py" in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:
                    f"Auto-configured --reasoning-parser {auto_config.reasoning_parser}"
                )

    # Initialize tool call parser if specified via CLI (or auto-detected)
    if args.tool_call_parser:
        global _enable_auto_tool_choice, _tool_call_parser, _enable_tool_logits_bias
        _tool_call_parser = args.tool_call_parser
        _enable_auto_tool_choice = True  # Implied by --tool-call-parser
        logger.info(f"Tool call parser enabled: {args.tool_call_parser}")
    if args.enable_auto_tool_choice:
        _enable_auto_tool_choice = True
    if args.enable_tool_logits_bias:
        _enable_tool_logits_bias = True

    # Initialize reasoning parser if specified (or auto-detected)
exec
/bin/zsh -lc "sed -n '1,80p' vllm_mlx/tool_parsers/__init__.py && printf '\\n---\\n' && sed -n '1,80p' vllm_mlx/tool_parsers/deepseek_tool_parser.py" in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:
# SPDX-License-Identifier: Apache-2.0
"""
Tool call parsers for vllm-mlx.

This module provides tool call parsing functionality for various model formats.
Inspired by vLLM's tool parser architecture but simplified for MLX backend.

Available parsers:
- auto: Auto-detecting parser that tries all formats (default)
- mistral: Mistral models ([TOOL_CALLS] format)
- qwen/qwen3: Qwen models (<tool_call> and [Calling tool:] formats)
- llama/llama3/llama4: Llama models (<function=name> format)
- hermes/nous: Hermes/NousResearch models
- deepseek/deepseek_v3/deepseek_r1: DeepSeek models (unicode tokens)
- kimi/kimi_k2/moonshot: Kimi/Moonshot models
- granite/granite3: IBM Granite models
- nemotron/nemotron3: NVIDIA Nemotron models
- xlam: Salesforce xLAM models
- functionary/meetkai: MeetKai Functionary models
- glm47/glm4: GLM-4.7 and GLM-4.7-Flash models
exec
/bin/zsh -lc "sed -n '193,202p' README.md" in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:
|-------------|---------------------|---------------------|-------|
| Qwen3.5 (all sizes) | `hermes` | `qwen3` | **Recommended** — 100% tool calling |
| Qwen3-Coder-Next | `hermes` | *(none)* | Fast coding, non-thinking mode |
| GLM-4.7 | `glm47` | *(none)* | 100% tool calling |
| MiniMax-M2.5 | `minimax` | `minimax` | XML tool format |
| GPT-OSS | `seed_oss` | `gpt_oss` | Native format |
| DeepSeek-R1 | `deepseek` | `deepseek_r1` | With reasoning |
| Llama 3.x | `llama` | *(none)* | JSON tool format |
| Mistral | `hermes` | *(none)* | Hermes-compatible |

exec
/bin/zsh -lc "nl -ba vllm_mlx/model_auto_config.py | sed -n '20,75p'" in /Users/raullenstudio/work/vllm-mlx succeeded in 53ms:
    20	
    21	
    22	# Model family patterns → optimal config.
    23	# Order matters: first match wins. More specific patterns go first.
    24	_MODEL_PATTERNS: list[tuple[re.Pattern, ModelConfig]] = [
    25	    # DeepSeek (R1, V3) — before Qwen because DeepSeek-R1-Qwen3 distills exist
    26	    (re.compile(r"deepseek", re.IGNORECASE), ModelConfig(
    27	        tool_call_parser="hermes",
    28	        reasoning_parser="deepseek_r1",
    29	    )),
    30	    # Qwen family (Qwen3, Qwen3.5, Qwen3-Coder)
    31	    (re.compile(r"qwen3", re.IGNORECASE), ModelConfig(
    32	        tool_call_parser="hermes",
    33	        reasoning_parser="qwen3",
    34	    )),
    35	    # GLM family (GLM-4.5, GLM-4.7)
    36	    (re.compile(r"glm[-_]?4", re.IGNORECASE), ModelConfig(
    37	        tool_call_parser="glm47",
    38	        reasoning_parser=None,
    39	    )),
codex
The new auto-detection table assigns incorrect defaults for at least DeepSeek and Llama models, and it also enables a reasoning parser for Qwen3-Coder models that are documented as non-thinking. Those cases change runtime behavior for supported model families and can break tool calling or output parsing.

Full review comments:

- [P1] Use the DeepSeek tool parser for DeepSeek models — /Users/raullenstudio/work/vllm-mlx/vllm_mlx/model_auto_config.py:26-28
  Any model path containing `deepseek` now auto-selects `hermes`, but this codebase has a dedicated `deepseek` parser for DeepSeek’s unicode tool-call tokens and the README documents `deepseek` as the recommended parser for DeepSeek-R1. In the common case where a DeepSeek model actually emits its native tool format, auto-configuration will fail to parse tool calls unless the user overrides it manually.

- [P2] Exclude Qwen3-Coder from the generic Qwen3 reasoning config — /Users/raullenstudio/work/vllm-mlx/vllm_mlx/model_auto_config.py:30-33
  The broad `qwen3` match also catches `Qwen3-Coder-Next`, so serving that model now auto-enables the `qwen3` reasoning parser even though the README currently documents Qwen3-Coder-Next as a non-thinking model with no reasoning parser. That changes default streaming/output behavior for those models without the user opting in.

- [P2] Map Llama models to the llama parser instead of hermes — /Users/raullenstudio/work/vllm-mlx/vllm_mlx/model_auto_config.py:65-68
  Plain Llama models are auto-detected here but assigned `hermes`, while this repository’s parser registry and README describe Llama tool calling as a separate `llama` JSON format. For users serving Meta-Llama models without explicit flags, auto-configuration will select the wrong parser and break tool-call extraction.
